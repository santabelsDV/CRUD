const path = require('path');

module.exports = {
    'config': path.resolve('./database/config/config.js'), // Шлях до файлу конфігурації
    'migrations-path': path.resolve('./database/migrations'), // Шлях до папки міграцій
    'models-path': path.resolve('./database/models'), // Шлях до моделей
    'seeders-path': path.resolve('./database/seeders') // Шлях до сидів (якщо використовуєте)
};
